export class TodoList {
    constructor() {
        this.todos = [];
        console.log(" here is the todo list class");
    }

    newTodo(todo) {
        this.todos.push(todo);
    }

    deleteTodo(id) {
        if (this.id != id) {

        }
    }

    markAsCompleted(id) {

    }

    deleteAllCompleted() {
        
    }
}